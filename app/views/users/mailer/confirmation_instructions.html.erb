<p><%= t("users.login.mailer.welcome") %> <%= @email %>!</p>

<p><%= t("users.login.mailer.confirmaccountmessage") %></p>

<p><%= link_to t("users.login.mailer.confirmaccount"), confirmation_url(@resource, confirmation_token: @token) %></p>
